# 踩坑记录（docs/log · Issue #7 DoD）

> 持续沉淀开发/数据源/运维踩坑，供复盘引用。

## 2026-08-02 ~ 08-05

### 数据源类
| 坑 | 表现 | 根因 | 处理 |
|---|---|---|---|
| 东财 datacenter 报表名过期 | 龙虎榜/解禁/两融/大宗/股东/分红静默返回空 | 东财改版（code 9501 报表配置不存在）| curl 直测找新报表名 → patch（RPT_LIFT_STAGE/RPT_DATA_BLOCKTRADE 等）|
| push2ex 涨停池 rc=102 | 涨停池接口失效 | 东财风控（V3.6.0 已知）| 降级同花顺（数据一致）|
| Tushare 当日盘中 0 条 | 盘中 index_daily 空 | Tushare 盘后 17:00 才更新 | 盘中用腾讯/东财，盘后用 Tushare 复核 |
| Tushare forecast 不支持日期范围 | start/end 报参数错误 | 接口需 ann_date/ts_code 必填 | 逐日 ann_date 循环 |
| income 需 ts_code 必填 | 无法扫全市场中报 | Tushare 接口限制 | 用 disclosure_date 披露计划 + 单只查询 |
| 腾讯美股代码格式 | usNVDA.OQ 解析失败 | 腾讯美股用无后缀 usNVDA | 用 usNVDA（f3=现价 f4=昨收）|
| 腾讯分时格式 | 解析 0.0 | 分时点为空格分隔字符串 | split(" ") 解析 |
| adata 概念指数 close 为字符串 | 除法报错 | adata 返回 str | float() 转换 |
| 东财个股资金流 raise_for_status | 'dict' object has no attribute | em_get 返回 dict 非 Response | 去掉 raise_for_status |

### 工程类
| 坑 | 表现 | 根因 | 处理 |
|---|---|---|---|
| WSL cron 不自动启动 | 18:00 复盘静默失败 | WSL 服务不自启 | wsl.conf boot.command + bashrc 兜底 |
| crontab sed 污染 | & 被替换 | sed 替换符冲突 | Python 字符串替换 |
| reports/ 被 gitignore 但需提交 | 报告无法入库 | .gitignore 排除 | git add -f（公开后改回 git rm --cached）|
| GitHub 免费版分支保护 403 | API 配置失败 | 私有仓库需 Pro | 仓库公开后生效 |
| PR 作者不能 approve 自己 | merge 死锁 | GitHub 规则 | review count=0（CI 为门禁）|
| CI required checks 名不匹配 | merge 报 expected | check 名是中文显示名 | contexts 用实际 check 名 |
| 手动触发 workflow 被误列 required | merge 被拒 | 集成测试默认 skipped | 从 required contexts 移除 |
| 个人 Windows 路径硬编码 | 公开仓库泄露隐私 | 历史脚本残留 | 全部改环境变量/相对路径 |

### 时间/纪律类
| 坑 | 表现 | 根因 | 处理 |
|---|---|---|---|
| 时间戳推算/沿用 | 报告时间不准 | AI 手动推算 | real_time.py（腾讯 CDN 权威）+ 脚本统一 _rt() + AGENTS 强制规则 |
| 缓存旧数据 | 复盘沿用盘中定格 | 不重新拉取 | 复盘每次重拉（post_close_update）|

## 2026-08-22

### 数据源类
| 坑 | 表现 | 根因 | 处理 |
|---|---|---|---|
| Tushare 深证成指(399001.SZ) amount 口径陷阱 | 上证+深成加总 14411 亿 vs 全市场 18923 亿，差约 4400 亿 | 深成指仅 500 成分股，amount≠全深市 | 两市成交用 `api.turnover()`（腾讯沪深合计）/FTShare 全市场口径；Tushare 序列仅看相对趋势 |
| market_api.kline() 返回字段序非标准序 | 按 [date,open,close,low,high] 解析得矛盾值（high<open） | 接口实际返回 [date,high,close,low,open,vol] | 收盘价与 Tushare/快照交叉验证后确认字段序；关键数值必须双源核对 |
| GitHub API 匿名限流 403 | api.github.com releases 接口 403 | 未带 token 匿名调用超限 | 改 raw.githubusercontent.com 拉 tag 文件 |

### 工程类
| 坑 | 表现 | 根因 | 处理 |
|---|---|---|---|
| 新脚本裸 requests.get 被 CI 拦截 | PR #36 首跑 fail（theme_preview_1430.py:26） | 项目编码规范要求封装 Session+trust_env=False | `s=requests.Session(); s.trust_env=False; s.get(...)`；本地先复跑 CI 同款 grep 再推送 |
| a-stock-data SKILL.md 内嵌代码块无法整块 exec | 文档型代码混排 markdown 说明，exec 报 SyntaxError（全角字符） | 单文件 skill 是"文档+代码"，不是纯模块 | 验证走正则提取单函数 + 注入依赖（SH_INDEX 等），或按文档等价实现实跑真实接口验证 |

### 版本升级类
| 坑 | 表现 | 根因 | 处理 |
|---|---|---|---|
| AGENTS.md 版本号与技能实际版本漂移 | AGENTS 写 V3.6.0，实际已 3.6.1 | 上次升级只改了 verify 脚本没同步 AGENTS | 升级时三处同步：双份 SKILL.md + verify `_EXPECTED_VERSION` + AGENTS.md 全部版本引用（grep 旧版本号清零）|
